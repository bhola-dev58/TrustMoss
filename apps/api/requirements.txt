fastapi==0.115.0
uvicorn[standard]==0.30.6
python-dotenv==1.0.1
httpx==0.27.2
groq==0.11.0
moss==1.9.1
pydantic==2.9.2
livekit-api>=1.2.0
pyjwt>=2.14.0
cryptography>=43.0.0

# ── Phase 8: Secrets Management (optional — only needed per provider) ─────────
# Vault provider: pip install hvac
hvac>=2.3.0       # HashiCorp Vault KV v2 client
# AWS provider:  pip install boto3
# boto3>=1.35.0  # Uncomment if using AWS Secrets Manager

# ── Phase 9: PostgreSQL & Redis Database Layer ──────────────────────────────
asyncpg>=0.31.0   # Async PostgreSQL client library
redis>=5.0.0      # Async Redis client for session caching & circuit breaker
alembic>=1.13.0   # Database migration engine
sqlalchemy>=2.0.0 # Required by Alembic for schema migrations
