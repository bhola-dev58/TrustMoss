# ─────────────────────────────────────────────────────────────────────────────
# TrustMoss API — Development & Test Dependencies
#
# These packages are ONLY for local development, testing, linting, and CI.
# They are NOT installed in production Docker images (see Dockerfile target stages).
# ─────────────────────────────────────────────────────────────────────────────

# ── Test Runner ───────────────────────────────────────────────────────────────
pytest>=9.1.1
pytest-cov>=6.0.0
pytest-asyncio>=0.24.0
httpx==0.28.1          # FastAPI TestClient transport (already in requirements.txt — pinned here for dev)

# ── Linting & Formatting ──────────────────────────────────────────────────────
ruff==0.16.7            # Fast Python linter (replaces flake8 + isort + pyflakes)

# ── Security Static Analysis ──────────────────────────────────────────────────
bandit==1.9.4         # OWASP-aligned Python SAST scanner
pip-audit==2.10.1       # Supply chain CVE scanner (PyPA official)
