# ─────────────────────────────────────────────────────────────────────────────
# TrustMoss API — Development & Test Dependencies
#
# These packages are ONLY for local development, testing, linting, and CI.
# They are NOT installed in production Docker images (see Dockerfile target stages).
# ─────────────────────────────────────────────────────────────────────────────

# ── Test Runner ───────────────────────────────────────────────────────────────
pytest==9.1.1
pytest-cov==5.0.0
pytest-asyncio==0.24.0
httpx==0.27.2          # FastAPI TestClient transport (already in requirements.txt — pinned here for dev)

# ── Linting & Formatting ──────────────────────────────────────────────────────
ruff==0.6.9            # Fast Python linter (replaces flake8 + isort + pyflakes)

# ── Security Static Analysis ──────────────────────────────────────────────────
bandit==1.7.10         # OWASP-aligned Python SAST scanner
pip-audit==2.7.3       # Supply chain CVE scanner (PyPA official)
